# Документация проекта: Telegram-бот для мониторинга Gmail

## Обзор проекта

Этот проект представляет собой Telegram-бот, предназначенный для мониторинга входящих писем в аккаунте Gmail пользователя. 
Бот позволяет подключить Gmail-аккаунт через OAuth-авторизацию, мониторит новые письма в реальном времени и отправляет уведомления в Telegram о важных сообщениях. 
"Важность" определяется на основе категорий, которые пользователь может настроить (например, фильтры по типам писем: форумы, реклама, социальные сети и т.д.). 
Для классификации писем используется машинное обучение (линейный SVM-классификатор), обученный на очищенных текстах email.

### Основные функции:
- **Авторизация через OAuth**: Пользователь подключает свой Gmail-аккаунт без передачи пароля.
- **Мониторинг email**: Бот отслеживает новые письма с помощью истории изменений Gmail и отправляет уведомления в Telegram.
- **Классификация писем**: Используется ML-модель для категоризации email (форумы, реклама, социальные сети, обновления, коды верификации, спам).
- **Настраиваемые фильтры**: Пользователь выбирает категории для уведомлений через inline-кнопки в Telegram.
- **Уведомления**: Для каждого нового письма отправляется сообщение с отправителем, датой, темой и кратким содержанием.
- **Хранение состояний**: Токены авторизации и состояние хранятся локально на сервере.

### Технологический стек:
- **Язык**: Python 3.12+.
- **Библиотеки**:
  - Aiogram: Для создания Telegram-бота.
  - Google API Client: Для взаимодействия с Gmail API.
  - Google Auth OAuthlib: Для OAuth-авторизации.
  - Flask: Для обработки OAuth-callback (веб-сервер для обратного вызова).
  - Scikit-learn (joblib, pickle): Для загрузки и использования ML-модели.
  - NLTK: Для предобработки текста (очистка email).
  - Pandas, Re: Для обработки данных.
- **ML-компоненты**: Линейный SVM-классификатор с TF-IDF-векторизацией (модель сохранена в `ML/models`).
- **Контейнеризация**: Docker (docker-compose.yml).
- **Деплой**: Поддержка Gunicorn + Nginx для продакшена.

## Архитектура проекта

Проект разделен на модули для разделения ответственности:

1. **main.py**: Точка входа. Инициализирует все сервисы, запускает Flask для OAuth-callback, мониторинг email и polling Telegram-бота.
2. **telegram_handlers.py**: Обработчики команд Telegram (/start, /help, /auth, /filters). Управляет inline-меню для выбора категорий.
3. **email_oauth.py**: Сервис для генерации URL авторизации и получения токенов через OAuth.
4. **gmail_client.py**: Клиент для Gmail API: создание сервиса, получение профиля, истории изменений и сообщений.
5. **email_monitor_service.py**: Сервис мониторинга: циклически проверяет историю изменений для всех подключенных пользователей и отправляет уведомления.
6. **oauth_callback_app.py**: Flask-приложение для обработки OAuth-callback (получение кода, сохранение токенов, HTML-страница успеха).
7. **predictor.py**: Классификатор email с использованием ML-модели (загружает модель, предсказывает категорию).
8. **preprocessor.py**: Функции предобработки текста (очистка от стоп-слов, URL, email и т.д. с NLTK).
9. **metadata.json**: Метаданные модели (точность, категории и т.д.).
10. **docker-compose.yml**: Конфигурация Docker.
11. **oauth_config.py**: Константы для OAuth (scopes, client_secret, redirect_uri).

### Поток работы:
1. Пользователь в Telegram вводит /auth -> Бот генерирует OAuth-URL.
2. Пользователь авторизуется в Google -> Callback на Flask-сервер -> Сохранение токена и historyId.
3. Мониторинг: Каждые 60 сек проверка истории для каждого пользователя -> Если новые письма, классификация -> Уведомление, если категория в фильтрах.
4. Пользователь настраивает фильтры через /filters → Сохранение в JSON.

## Установка и настройка

### Требования:
- Python 3.12+.
- Установленные библиотеки: `pip install aiogram google-auth-oauthlib google-api-python-client flask joblib pickle nltk pandas re`.
- NLTK: `nltk.download('stopwords')`.
- Client Secret: Скачайте `client_secret.json` из Google Cloud Console (включите Gmail API).
- .env-файл: `BOT_TOKEN=your_token`.

### Локальный запуск:
1. Клонируйте репозиторий.
2. Установите зависимости: `pip install -r requirements.txt` (если есть; иначе вручную).
3. Запустите: `python main.py`.
   - Flask запустится на порту 8083.
   - Бот начнет polling.

### Docker-запуск:
1. `docker compose up -d --build`.

### Продакшн-деплой (пример с Yandex Cloud или VPS):
- Используйте Gunicorn для Flask: `gunicorn -w 4 -b 127.0.0.1:8083 bot.src.utils.oauth_callback:app`.
- Nginx для reverse-proxy и SSL.
- Домен: `oauth.hsepythonproject.ru` для redirect_uri.

## Использование

### Команды Telegram:
- `/start`: Приветствие и меню.
- `/help`: Список команд.
- `/auth`: Генерация ссылки для подключения Gmail.
- `/filters`: Настройка категорий (inline-меню с выбором: форумы 🗣️, реклама 🛒, соцсети 📱, обновления 🔄, коды верификации 🔐).

### Настройка фильтров:
- Выберите категории через кнопки.
- Сохраните: Уведомления только по выбранным.
- Сброс: Очистить все.

### ML-классификация:
- Модель: Linear SVM (точность 98.4%).
- Категории: forum, promotions, social_media, spam (не присылается пользователю), updates, verify_code.

## Подключение к почте и протокол

### Как подключаемся к почте?
Подключение осуществляется через **OAuth 2.0** для безопасной авторизации без передачи пароля. Процесс:
1. Бот генерирует URL авторизации (`generate_auth_url` в `email_oauth.py`).
2. Пользователь переходит по URL, авторизуется в Google.
3. Google перенаправляет на callback-URL (Flask в `oauth_callback_app.py`).
4. Получаем код, обмениваем на токены (`fetch_and_save_token`).
5. Токены сохраняются в JSON (`token_repositories.py`).
6. Создаем Gmail-сервис (`get_service` в `gmail_client.py`) и получаем доступ к API.

### По какому протоколу?
Не используется традиционные email-протоколы вроде IMAP, POP3 или SMTP. Вместо этого применяется **Gmail API** (RESTful API на базе HTTP/HTTPS). 
- **Протокол**: HTTPS (HTTP over TLS) для всех запросов.
- **Почему API?**: Позволяет эффективно мониторить изменения (используется polling каждые 60 сек).
- Преимущества: Безопасно, не требует хранения пароля.
